import { RequestHolder } from './requestHolder';

export class GitHubApiHelpers extends RequestHolder {
    async forkRepository(): Promise<void> {
        /**
         * Here you can implement the logic to fork a repository
         */
    }
}
